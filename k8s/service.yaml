apiVersion: v1
kind: Service
metadata:
  name: transaction-service
  namespace: fraudshield
spec:
  selector:
    app: transaction-service
  ports:
    - protocol: TCP
      port: 8003
      targetPort: 8003
  type: ClusterIP
